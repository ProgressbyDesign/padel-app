/** Homepage “top rated” / favourites threshold (coaches & venues). */
export const TOP_RATED_MIN_SCORE = 4.9;

/** Max items in homepage coach / venue carousels when filtering by rating. */
export const TOP_RATED_SECTION_LIMIT = 10;

/** Marketplace PLP page size (venues + coaches). */
export const LISTING_PAGE_SIZE = 50;

/** @deprecated Use LISTING_PAGE_SIZE */
export const VENUES_PAGE_BATCH_SIZE = LISTING_PAGE_SIZE;
